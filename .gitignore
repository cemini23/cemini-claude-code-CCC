# Transient state — never commit
hot.md
.claude/
.env
.env.local

# Local-only data (root prod briefs only — wiki/briefs/ cross-wiki handoffs are tracked)
/briefs/
raw-sources/
research to be indexed/

# Python / tooling
__pycache__/
*.pyc
.venv/
node_modules/

# OS junk
.DS_Store
Thumbs.db
*.swp
*.swo

# Local Phase-0 clones (not tracked)
.local/
