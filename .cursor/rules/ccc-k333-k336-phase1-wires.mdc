---
description: CCC Phase-1 wires from K333–K336 harness wave (CCC-only — do NOT federation-sync)
alwaysApply: true
---

# CCC — K333–K336 Phase-1 wires

**Brief:** `docs/briefs/2026-09-02_k333-k336-harness-wave.md` · SIP: `wiki/briefs/2026-09-02_ccc-k333-k336-sip-ready.md`

**Dual-ID (resolve by file + wiki, not bare K#):** **CCC K333** (2609.00267 delegation/trust) ≠ **CCC K285 Mandato** / **K310 AP2** / **K326 external monitor** / **K328 attestation** — Cybersec may take K333 as primary steal. Do not delete BPS / Wayfinder / AID-Guard / K310–K332 blocks. Executor: OpenCode Zen free pick then **claude-ds Flash** — never Pro.

- **K333** Delegation Without Trust — **Cybersec-primary** + **CCC ADOPT policy**: **untrusted-model property** — a governed system must confine a fully prompt-injected agent to explicitly delegated authority; authorization broker **external** to model reasoning; four threats (confused deputy, token theft/replay, PI privilege escalation, compromised sub-agents); default frameworks (LangGraph, CrewAI, AutoGen, MCP auth) lack built-in confinement. VotalAI LLM Shield production reference — **no public SPDX → policy only, no clone. No PoCs.** `policy_wired`. Pairs K285/K310/K326/K328.
- **K334** HarnessDev — **ADOPT eval-first**: unit of evaluation = **runnable harness**, not task output; **Creation** (weak seed → full harness) + **Evolution** (downstream feedback); generated harnesses lag human references on code/search; evolution gains **unstable**, partial held-out transfer, **executor-model-dependent**; state/memory components often **never fire** (0 checkpoint events / 26k trajectories). Project page only — **no public SPDX → policy only**. Runtime **`wont_wire`**. Pairs K281/K292/K313/K332. **Never auto-evolve `.cursor/skills`.**
- **K335** Harness-of-Harness — **ADOPT pattern**: meta-harness **on existing** coding-agent harnesses; iterative plan–code–test loops; balance repair with capability growth; small verifiable increments; **separate implementation testing from independent evaluation**; versioned project history; progressive tool/skill exposure. +52.25% avg relative gain after 3 iterations. `Flesymeb/HarnessOfHarness` **MIT** ~312MB → **GO REFERENCE** `.local/adopts/HarnessOfHarness`. Runtime **`wont_wire`** — do not replace Cursor/`/route`. `policy_wired`. Pairs K281/K292/K313/K318.
- **K336** CordisBench — **ADOPT eval axis**: dynamic harness plugin changes propagate via **dependencies + teardown order**; models degrade as relevant interactions scale (2→32); when formal spec available, **deterministic reference semantics / Cordis execution** beats LLM anticipation (528/528 agreement). GitHub+HF cited — **no public SPDX at Phase-0 → WATCH**, no clone this wave. Eval runtime **`wont_wire`**. Pairs K327 Logos. Concept `policy_wired`.

**Wires landed:**

| K | Verdict | Wire | Entity/concept wire_status |
|---|---------|------|----------------------------|
| K333 | ADOPT policy (Cybersec-primary) | External broker; untrusted-model property; scoped sub-agent delegation | `policy_wired` (untrusted-model-delegation-governance) |
| K334 | ADOPT eval-first | Evaluate harness infrastructure; frozen held-out + executor swap before persist | `policy_wired` (harness-as-eval-artifact); runtime `wont_wire` |
| K335 | ADOPT pattern | Meta-harness wrap; independent eval gate; versioned increments | concept `policy_wired`; entity `wont_wire` (HarnessOfHarness REFERENCE) |
| K336 | ADOPT eval axis | Teardown-order reasoning; prefer execution verify at scale | `policy_wired` (harness-component-lifecycle-reasoning); runtime `wont_wire` |

**Leftovers / non-goals:** no federation sync of this file; no HarnessDev / CordisBench / VotalAI clones; no `.cursor/skills` auto-evolution; no `route-task.ps1` rewrite; no `~/.claude/settings.json` edits; no PoCs / attack templates anywhere.
