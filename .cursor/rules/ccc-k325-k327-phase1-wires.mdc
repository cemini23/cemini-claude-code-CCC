---
description: CCC Phase-1 wires from K325–K327 harness wave (CCC-only — do NOT federation-sync)
alwaysApply: true
---

# CCC — K325–K327 Phase-1 wires

**Brief:** `docs/briefs/2026-08-31_k325-k327-harness-wave.md` · SIP: `wiki/briefs/2026-08-31_ccc-k325-k327-sip-ready.md`

**Dual-ID (resolve by file + wiki, not bare K#):** Same-day Cybersec inbox has **2608.28490** and **2608.28502** — CCC **K325/K326** vs Cybersec's own K#. **CCC K285 Mandato** / **K310 AP2** / **K312 StepGate** remain distinct from K326 external monitor — do not merge. Do not delete BPS / Wayfinder / AID-Guard leftover / K310–K324 blocks. Executor note: OpenCode `x-preview-f-free` unsupported → Flash executed this wave.

- **K325** LLM-Based Agents for Software and Systems Security (SLR) — **Cybersec-primary**, 72-page survey. CCC steal: agents are built to **act** (91% modular, 88% planner-and-actor, 95% autonomous) but not to be **bounded** (15% critic/verifier, 13% guardrail, 13% human review) or **audited** (11% hallucination, 5% scope containment, 1% tool-call accuracy). CCC keeps HITL on irreversible tools (pairs K239); evaluates with evidence trees not scalars (pairs K289); treats bounded authority + auditable trajectories as the design gap (pairs K277). Short CCC source + one concept — no survey dump. Runtime `wont_wire`.
- **K326** Recognition Without Enforcement — **ADOPT policy**: model self-arbitration is a **capability**, not a security boundary. Recognition (decodable source-format + verbalized detection) ≠ enforcement. Fleet-mean execution under novel spoofs is low (1.21% [0.5–2.1%]) but **concentrated in reproducible cells** and shifts up to 47pp across deployment windows; restrictive policy eliminates execution on the same weights. External reference monitor = **authenticated source routing** (HMAC-SHA256, deterministic under EUF-CMA) + **capability-gated tool execution** (fail-closed external layer, single-use nonce tokens). Residual gap: **semantic authorization** of authenticated user/deputy messages (confused deputy — pairs K310 pre-auth intent). Pairs K285 Mandato, K312 StepGate. `policy_wired`. **Zero attack text. Do not clone InstructionArbitrationBench until SPDX. Do not paste paper prompts.**
- **K327** Logos — **ADOPT policy**: plugin = **process**; only shared state = **append-only transcript**; resume with **no repeated effect** after kills at the four tool-call-cycle boundaries (during execution / after return before persistence / after persistence before announcement / after announcement). 80/80 sessions resume clean; one fault ends at one node vs single-process SPOF. Pairs K282 AgentRewind recovery-boundary honesty + K239 execution-fidelity invariants. No public SPDX → **policy only, no clone**. Runtime `wont_wire` — do not replace Cursor/`/route`.

**Wires landed:**

| K | Verdict | Wire | Entity wire_status |
|---|---------|------|--------------------|
| K325 | Cybersec-primary steal | HITL on irreversible tools; evidence-tree eval | `wont_wire` (survey) |
| K326 | ADOPT policy | External enforcement for high-blast tools; auth-routed sources; capability-gated execution | `policy_wired` (external-reference-monitor) |
| K327 | ADOPT policy | Isolate plugins; append-only durable state; no re-execution of retained prefix | `wont_wire` runtime (logos-agent-harness) |

**Leftovers stay propose-only (HITL first):** real Claude Code PreToolUse hook (`~/.claude/settings.json` — ask first); dense/BM25+vector SCOUT index. No hook install, no `route-task.ps1` rewrite, no `~/.claude/settings.json` edits, no `.cursor/skills` auto-evolution, no federation sync of this file.
