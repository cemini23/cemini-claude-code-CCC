---
description: Phase-1 policy wires — memory/HITL/codegen/tokenization/fidelity/sentinel/cost/GUI-MCP/input/canary/Argus/keystore/READ/PTC/CASCADE/ResidencyRL/BlastRadius/SHE/measurement/InterSAGE/MARC/Vero/AutoDesign (K230–K281)
alwaysApply: true
---

# Phase-1 policy wires (K230–K281)

Concrete harness behavior from Phase-0 ADOPT patterns. Clones under `.local/adopts/` are REFERENCE unless noted.

## Reconstruct before inject (MemHarness / K230)

- Before pasting prior `hot.md`, Discord, or trajectory snippets into a new turn: **rewrite for the current state** or drop them.
- Do not inject verbatim “temp” rules that outlived their context (negative transfer).
- Trainer stack `.local/adopts/MemHarness` = paper repro only — **not** a Cursor install.

## Build / use / bypass models (Tycho / K231)

- Do not auto-build solvers, simulators, or world-models on every verify failure.
- Prefer **actor-requested** (or advantage-gated) model construction; otherwise bypass and act.
- Sim transition-match ≠ next-action value. `.local/adopts/Tycho` = ARC eval REFERENCE.

## Passive multi-agent inbox (AgentRadio / K232)

- Parallel Task/subagents need a **non-blocking inbox** (file, thread, or MCP), not only phase-boundary handoffs.
- Mid-execution findings must be shareable without stopping foreground work.
- Harbor/Modal multi-agent fleet in `.local/adopts/AgentRadio` = paper repro — **not** wired to Modal.

## Claim-unit answers (AskChem pattern / K233 + K210)

- Prefer **claim + source locator** (DOI/URL/quote or wiki `@path`) over document dumps.
- If unsupported: say **NO_EVIDENCE** (Traceable Scholarship).
- Do **not** add AskChem chemistry MCP unless the operator explicitly asks. `.local/adopts/askchem` = REFERENCE client.

## Local-first MCP + HITL QC (CyberNeuro pattern / K234)

- Sensitive pipelines: prefer **local-first / no egress by default** and a pinned execution layer.
- Skills as typed contracts → **validate DAG** before parallel fan-out.
- High-blast QC: keep a **HITL verification panel** — automated metrics alone are not enough.
- CyberNeuro neuroimaging product **wont_wire** (OOD / no public repo).

## Mutation-consistency codegen tests (MCP-Tester / K235)

- When testing transpiler/codegen/agent rewrites without runnable binaries: use **mutation-consistency** metamorphic relations (predictable structural output change), not crash-only fuzz.
- Name collision: MCP-Tester here = Mutation Consistency of Programs — **not** Model Context Protocol.
- `.local/adopts/mcp-tester-transpiler` = Zenodo CC-BY-4.0 REFERENCE — do not runtime-wire into prod.

## Decision-coupled hierarchical memory (HAM-VLN / K236)

- In the same turn that chooses the next action, also record progress / failures / place semantics into durable state.
- Bound recent verbatim context; retrieve older via relevance / recency / salience (pairs K154 / K230).
- HAM-VLN robotics code **wont_wire** (no public repo).

## Agentic-engineer competencies (ACCEL / K237)

- Score operator workflows on: intent framing · orchestration · verification · ethical accountability · continuous learning.
- Watch automation bias, deskilling, superficial engagement, and felt-vs-measured speedup gaps.
- Paper/rubric only — no curriculum product install.
- **Dual-ID note**: today's meta-harness brief board also says **K237** (AutoDesign/Vero/misevolution) — resolve by arXiv id / brief path, not bare K# (see skill-misevolution subsection under SHE).

## Tokenization TTFT under high cache hits (TokTier / K238)

- At high prompt-cache hit rates, **tokenization** can dominate TTFT — measure front-end cost, not only prefill.
- Prefer serving stacks with **exact stateful incremental** tokenization when available; do not claim a TokTier deploy (no public artifact).

## Execution fidelity for irreversible writes (K239)

- LIVE Discord, trading egress, durable storage, or other irreversible effects: **preview/confirm before write**, pre-state check, post-state observe, **no phantom success**, scoped actor authority, **verify-then-retry** (not bare retry), idempotent external intents.
- Preview ≠ proven user intent — it confines the unbounded intent question to a finite object.
- Datumpont product **wont_wire** (no public repo).

## No raw LLM tabular prediction (K241)

- Do not use a single-pass LLM over pasted CSV/feature tables as primary predictive analytics — accuracy collapses with dimensionality.
- Prefer classical / tabular-foundation / solver tools; LLMs for narrative around tool outputs.
- **OSINT/GW K241** (Capital Flows 30y + three equity risks, alert-only) = `guruwatcher-k241-capital-flows-macro-phase1-wires.mdc` — do not collapse with this tabular K241.

## Telemetry + deterministic failure repair (Trajectory Sentinel / K242)

- Prefer **deterministic verification** (recompute claims from tool results + required-call coverage) before LLM-as-judge for mid-run failures.
- Cheap telemetry monitors OK when healthy-null is **recalibrated per deployment** (cold transfer fails).
- On alarm: rollback + re-run beats blind resample. `.local/adopts/agent-trajectory-sentinel` = MIT REFERENCE — not a default MCP install.
- **OSINT/GW K242** (Capital Flows 2026-08-19 equity positioning, alert-only) = `guruwatcher-k242-capital-flows-equity-positioning-phase1-wires.mdc` — do not collapse with this Sentinel K242.

## Agentic cost estimation vocabulary (ACEM / K243)

- Budget agent work as **LLM + HITL + infrastructure**; track Revision Factor (retries), Context Factor (growing context), HIS oversight level.
- Conceptual only — calibrate locally; do not treat the related Zenodo book as ACEM code.
- **OSINT/GW K243** (career-ops Extract + Klement no-arm + purple HOLD, alert-only) = `guruwatcher-k243-career-ops-klement-phase1-wires.mdc` — do not collapse with this ACEM K243.

## Robotics MCP solver (K240)

- `.local/adopts/MixedComplementarityProblems.jl` = BSD-3 REFERENCE for robotics/game-dev — **wont_wire** on CCC/TipDrop/prod harness.
- **OSINT/GW K240** (Talon HITL + RSSB + VIVO, alert-only) = `guruwatcher-k240-talon-rssb-phase1-wires.mdc` — do not collapse with this robotics K240.

## Clarify-before-act + evidence-aligned close (UrbanAgent / K244)

- Clarify missing critical inputs **before** tool fan-out.
- Ground tool choice in live observations; final reply must align with observed evidence + task constraints.
- Score execution quality (required-tool coverage, dependency validity, traceability), not success alone.
- UrbanAgent product **wont_wire** (no public repo / urban OOD).

## Hybrid GUI–MCP tool adoption gap (K245)

- Do **not** assume MCP/tool availability helps — same tools can lift reasoning models and hurt instruction-only policies.
- Measure the **adoption gap** (tool-reachable tasks vs actual calls), not only tool presence.
- After a successful text-tool call, drop the next screenshot / halve image history only when train and infer share that observation rule.
- OSWorld hybrid code **wont_wire** (no public artifact).

## Geospatial computation vs parametric knowledge (MultiGlobeQA / K246)

- Distances, containment, grid/shape answers need **solvers/tools**, not LLM geographic trivia (pairs K199 / K241).
- Gold facts can still fail if computation is missing; low-income region gaps may widen with retrieval.
- `.local/adopts/MultiGlobeQA` = MIT REFERENCE bench — **wont_wire** as a default eval harness.

## Voice vs keyboard agent input (HIVE / K247)

- Prefer **typed** operator input for constructive / deduced tasks; voice transcription structure (not fillers) drops accuracy.
- Destroying question tokens hurts; adding tokens costs little. Thinking budget recovers keyboard typos, **not** speech.
- HIVE suite **wont_wire** (no public code).

## Turn-level hindsight TIR (TurnSight / K248)

- Long-horizon tool RL: prefer turn-level, execution-conditioned hindsight over trajectory-only reward — **training pattern only**.
- `quchangle1/TurnSight` **NO-GO** clone (no SPDX / no LICENSE). **wont_wire** runtime.

## Edge MCP + meta-action executors (Scale-CDA / K249)

- GenAI proposes structured actions; **verified meta-action executors** enact them — do not let the model rewrite safety/policy cores.
- Automotive Scale-CDA / Openpilot stacks **wont_wire** (OOD / no Scale-CDA repo).

## Canary tools for MCP selection diagnosis (K250)

- Plant diagnostic canary tools (semantic decoys, parameter traps, capability mirages, prerequisite blindness, temporal decoys, granularity traps) when auditing MCP tool-selection.
- Capability tier ≠ safety; measure canary susceptibility rate (CSR). No public canary framework artifact — taxonomy only.
- Entity `canary-tools-mcp` = `policy_wired`.

## Split-inference activation compression (RAC / K251)

- Local–cloud–local splits bottleneck on boundary activations — prefer local head/tools for privacy-sensitive agents; WATCH codecs.
- RAC code **wont_wire** (no public artifact).

## Bidirectional HAI skill evolution (ArtAnno / BiHAA / K252)

- Close the loop: operator trajectories distill into skills/KB (pairs AFTER / skill-vetting) — not one-way AI assist.
- ArtAnno product **wont_wire**; skip Image-gen local wire.

## Verification-gated persist/pivot runtime (Argus / K253)

- Separate Manager/Planner/Engineer from independent Reviewer; admit memories/skills only after review (+ task-native verify when available).
- Persist while evidence supports; pivot on measured failure. Durable project state across sessions.
- `lbx154/Argus` **NO-GO** clone until LICENSE/SPDX present (pyproject MIT claim insufficient). **wont_wire**.

## Hardware-confined agent signing (K254)

- Do not put production signing/SSH/API private keys in agent-readable env, workspace files, or container memory for LIVE/high-blast agents.
- Prefer PKCS#11 / hardware (or OS-isolated) keystores so the host only sees opaque handles + signatures (pairs K239).
- SoftHSM = reproducibility proxy only — not tamper resistance. Anonymous paper artifact **NO-GO** clone until public SPDX.
- Entity `hardware-keystore-mcp-signing` = `policy_wired`.

## Interpretable agentic document ops (READ / K255)

- For long table-heavy / financial-style docs: prefer grep · outline · bounded span reads (or BM25) before dense top-k RAG.
- Require citable line/locator ranges in answers (pairs claim-centered / Traceable Scholarship).
- BM25 can match agentic READ — do not overclaim “agent beats lexical.”
- `.local/adopts/READ` = MIT REFERENCE; **wont_wire** as default CCC MCP.

## Trajectory error-lifecycle attribution (TrajDebug / K256)

- On failed long-horizon sessions: locate earliest wrong commitment with verbatim evidence, not only the terminal symptom.
- Prefer evidence-grounded attribution before LLM-as-judge for “what went wrong” (pairs K242).
- `.local/adopts/TrajDebug` = MIT skeleton (0 `.py` at Phase-0) — **wont_wire** until detector code ships.

## Anytime-valid agent eval stopping (AV-AIVAT / K257)

- Costly agent-vs-agent evals (esp. poker/IIG): prefer variance-reduced + anytime-valid confidence sequences over fixed budgets or naive CI peeking.
- Online value corrections must not use the current game. Release stop metadata for third-party recheck.
- No public AV-AIVAT artifact — pattern only; poker Arena brief. **wont_wire**.

## Programmatic vs JSON tool calling (PTC / K258)

- For code-capable lanes with a sandboxed executor: prefer typed stubs + script composition for multi-tool chains and fan-out (pairs code-as-agent-harness / Code Mode).
- Keep JSON schemas for weak models or when code exec is banned; stubs remain schema-bound / Prebind-gated.
- No public PTC eval harness — **policy_wired** only. Entity `programmatic-tool-calling-ptc`.

## MCP claim validation + NL→tool parameter grounding (CASCADE / K259)

- Before LIVE/high-blast MCP exposure: validate the **claim class** the tool report repeats against ground truth or a stated proxy — membership checks are not enough (pairs K172 / Traceable Scholarship).
- Prefer **server-side** alias/enum normalization over trusting model-filled strings.
- Ambiguous required fields: clarify or refuse — do not silent-default; “if unset” guards miss confident wrong fills (pairs K244).
- Small local models need stricter schema enforcement; larger models reduce schema fails but not confident-wrong defaults.
- `.local/adopts/CASCADE` = MIT REFERENCE (cancer genomics OOD) — **wont_wire** runtime; do not download DepMap/model into the adopt shelf.
- Entity `cascade-mcp` = `wont_wire`; concept `mcp-claim-validation-and-parameter-grounding` = `policy_wired`.

## Adversarial curriculum + doc fidelity (ResidencyRL / K260)

- Long-horizon / multi-turn evals and red-team packs: include **adversarial** and **information-asymmetric** cases, not only cooperative happy-path.
- Score with rubrics **plus** hard fidelity/safety penalties (fabricated durable facts, missed required checks, unsafe shortcuts).
- Gate `done`/terminate on **required-check coverage** — resist premature closure.
- Durable artifacts (notes, Discord, wiki claims, tool reports): **elicited/observed facts only** — never invent fields to fill a schema (pairs Traceable Scholarship / K259).
- Required clarifications under deflection: re-ask once with rationale, then refuse/escalate — do not invent answers (pairs K244).
- Soft autoraters OK as advisory signal; prefer execution verify when available (pairs K162). ResidencyRL code **NO-GO** (Google closed; clinical OOD) — no `.local/adopts/` shelf.
- Entity `residencyrl` = `wont_wire`; concept `adversarial-curriculum-structured-reward-doc-fidelity` = `policy_wired`.


## Reversible context eviction (Blast Radius / K265)

- Prefer **reversible, verification-licensed** eviction over lossy truncation/summarization in long agentic coding loops.
- Estimate prompt reach (context retention + churn-weighted code surface) before sweeping dead mass.
- Archive buried bodies byte-exact (midden) with secret redaction; leave scent skeletons; bound exhumation cost.
- Treat recurring build/test/VCS transcripts as **recurring dead matter** — bury recurrence classes, do not re-pay carry cost every turn.
- Layering: gate/verification decides *whether* a record may replace context; blast-radius scoping decides *which* bodies and *how much* headroom.
- No public Chalk/Blast Radius SPDX clone at Phase-0 — **policy_wired** pattern only; entity `blast-radius-necrophoresis` = `wont_wire`.


## Scaffold over tool interface (K262)

- When measuring MCP vs CLI (or any transport): treat **agent scaffolding** as the primary axis — prompts, control flow, memory, verification — not the interface brand.
- Prefer completion checks via **external state** (repo/tests/prod observe), not agent self-report.
- MCP is not required for every task class; CLI-only scaffolds can be complete (paper 2608.08654).

## Versioned model–harness continual adaptation (Macaron / K263)

- Prefer **versioned model+harness pairs** with parent/data/eval provenance when iterating agent stacks.
- Successor construction under an **external** eval contract — no closed loop that rewrites its own pass criteria (pairs K162).
- Mixture-of-LoRA / specialist adapters = vocabulary only — **no** Macaron/Venti weight downloads; `hcp-sdk` null SPDX **NO-GO** clone.

## LLM verification layer accept/reject/escalate (K264)

- High-blast plans: gate with ternary **accept / reject / escalate** before irreversible MCP/tool effects (pairs K239/K244).
- Prefer independent review of judge **reasoning quality** under disagreement — do not silent-accept.
- Robot `verification_layers` repo **404** — pattern only; entity `wont_wire`.

## Trajectory-driven safety harness evolution (SHE / K268)

- Name the owning harness artifact before edits: **System Prompt / Rule Bank / Safety Memory / Tool Policy**.
- Attribution-guided, artifact-local edits + safety–utility validation; **HITL** before mutating prod Cursor/Claude harnesses.
- `.local/adopts/SHE` Apache-2.0 REFERENCE (~5MB) — **do not** unattended auto-evolve prod. Entity `she-safety-harness` = `wont_wire` runtime; concept `policy_wired`.

## Skill misevolution lifecycle gates (arXiv 2608.12851 · board K237)

- **Skills can worsen with practice** — a terminal success-rate cannot distinguish a clean skill library from an unsafe skill never retrieved (21/21 evolved configs authored unsafe artifacts; 15 retained fresh-session harm).
- **Govern all three lifecycle gates** — authoring (CU/UG/Stealth), retrieval (URR), execution (C-ASR). **HITL on a skill's write does not cover retrieval-time harm** — a later retrieval can activate an unsafe span; keep retrieval lineage + retirement on evidenced harm.
- **Prefer delete-only repair + reuse-time attribution** (SAFEEVOLVE: delete-only repair critic, reuse-time attribution, retire after 2 harmful reuses, library cap); **no unattended auto-evolve of `.cursor/skills/*`** promote/refine cycles.
- **Dual-ID note**: ACCEL is also labeled **K237** in this file; today's meta-harness brief board **K237** = AutoDesign/Vero/misevolution — resolve by arXiv id / brief path, not bare K#.

## VectraYX cyber VLM (K261)

- Spanish/LATAM cyber imagery VLM with MCP tool tokens — **cybersec-primary**; CCC awareness only. **NO-GO** weights/runtime.


## CCC wave K270–K273 (shared policy file — dual-ID note)

Same-day Cybersec batch used **K270–K272** for GFlowNet / REDAgentBench / Cross-lingual safety in `cemini-cybersec-lab-redteam.mdc` + `cemini-cybersec-agent-audit.mdc`. Numbers below are **CCC** MCP/ACM-wave meanings in this shared federation file (also `ccc-k270-k273-phase1-wires.mdc`). Resolve by file+wiki, not bare K# alone.

## LLM-assisted writing hygiene (CCC K270)

- Treat LLM-writing detection as advisory for wiki/brief hygiene — prefer claim+locator (Traceable Scholarship).
- Do not overclaim human-only authorship. `kobaklab/llm-usage-in-pmc` null SPDX **NO-GO** clone.

## Enterprise MCP auth gateway (CCC K271)

- Prefer a **central auth aggregation layer** when many MCP servers proliferate (prod/enterprise).
- Classify callers on **persona** (user vs non-user/service) × **credential** path before exposure.
- Plan fleet offboarding / token revocation across servers — not per-server snowflakes.
- Industry paper only — no mandatory gateway product install from this source.

## Domain solvers as MCP + Skills (CCC K272)

- Prefer wrapping verified domain solvers as **MCP tools + SKILL.md** rather than asking the LLM to recompute science.
- Pass skill-vetting / preflight before enable. `.local/adopts/agent4binary` MIT REFERENCE (~185MB) — astronomy runtime **wont_wire**.

## Agentic Configuration Management / ACM (CCC K273)

- Treat harness configs (rules, skills, MCP allowlists, settings) as **configuration items** with baselines + provenance.
- Prefer deterministic validation and dependency-aware impact before blast-radius harness edits.
- No ACM product install until a clear SPDX'd artifact exists.

## CCC wave K274–K276 (shared policy file — dual-ID note)

Same-day Cybersec batch used **K274–K277** for WhiteNet / wireless AInf / tutor-withhold / RSM in `cemini-cybersec-lab-redteam.mdc`, `cemini-cybersec-agent-audit.mdc`, and `cemini-cybersec-mcp-tool-control.mdc`. Numbers below are **CCC** harness-wave meanings (CDH / VAKRA / RSM). **RSM paper 2608.12311 = CCC K276 AND Cybersec K277** — resolve by file+wiki, not bare K#.

## Convergent Detour Hijacking / CDH (CCC K274)

- **Trajectory necessity is a security requirement** — final-answer correctness does not guarantee cost safety. Audit trajectories, not just outputs (pair K259 traceable scholarship).
- Skill **selection metadata + instruction body are coupled control points** in progressive disclosure — one static skill can bridge the boundary.
- **Pre-installation review**: routing claims must match declared skill role; body deps unrelated to role are a red flag.
- **Runtime monitors**: flag unexplained cross-skill transitions; enforce token/invocation budgets for untrusted skills (budgets don't assume harmful behavior changes the output).
- CDH is a text-only attack pattern — **no code/SPDX**; policy threat model only.

## Multi-hop API + retrieval eval under tool-use policies (VAKRA / CCC K275)

- **Eval axes**: API interaction paradigm (endpoint vs compositional), reasoning depth (2–5 hops), multi-source grounding, NL tool-use policy constraints.
- Failures concentrate at **language-mediated reasoning** (entity disambiguation, cross-source grounding), not tool mechanics — measure those.
- Policy-constrained / unanswerable queries are the hardest signal (2.4% frontier) — eval must include refusal/unanswerable detection.
- Prefer **re-exec verification** against live APIs over self-report.
- `IBM/vakra` **NO-GO clone** (CC-BY-NC-SA 4.0, verified LICENSE file) — eval axes ADOPT, HF dataset WATCH only.

## Role Specialization Model / RSM (CCC K276 · Cybersec K277)

- **Explicit role distribution per tool** with stated responsibility domains; expect drift from absent scope boundaries, capability overlap, context-switching cost — orchestrator manages it.
- **HITL verification of agent-generated outputs** before durable-artifact integration (no pure vibe coding).
- **Prompt-hardening via explicit negative constraints** when agents with tool access attempt unavailable internal tools.
- **Failure-backed instructions only** — over-detailed rule files reduce success + raise cost (pairs K273 ACM baseline discipline).
- RSM is a case-study framework — no product install; policy pattern only. Cybersec domain wire: `cemini-cybersec-mcp-tool-control.mdc` §K277.

## CCC wave K277–K281 (shared policy file — dual-ID note)

Same-day Cybersec batch used **K278–K281** for ATOBench / MARC / YAVIN / ente in `cemini-cybersec-lab-redteam.mdc`, `cemini-cybersec-agent-audit.mdc`, `cemini-cybersec-mcp-tool-control.mdc`, and `cemini-cybersec-agent-containment.mdc`. Numbers below are **CCC** meanings. **CCC K278 = InterSAGE**, **Cybersec K278 = ATOBench**. **MARC-v1 2608.13476 = CCC K279 AND Cybersec K279** (clinical `wont_wire`, orchestration REFERENCE). Resolve by file+wiki, not bare K#.

## Measurement integrity in MCP security eval — labels ≠ endpoints (CCC K277)

- **Labels are not behavioral endpoints.** A security-eval label ("attack") is a claim over treatment bytes, executed behavior, authorization, outcome rule, and analysis unit — bind all five before interpreting it.
- **Treatment-blind grading**: graders must not see treatment metadata when assigning outcome classes. Treatment invariance is necessary, not sufficient — codebook ambiguity survives it (4 construct-boundary cases in K277).
- **Seven-link Integrity Chain**: each link has a verification question, minimum evidence, and a fail-closed condition; a failed link halts the stated inference. Use the scope-bounded endpoint-integrity linter for mechanically testable measurement defects.
- **Scope honesty**: a campaign measurement audit is not a population attack-rate, model-ranking, defense-efficacy, or causal estimate. Don't convert campaign counts into security claims they can't support.

## InterSAGE trust-native IoA protocol (CCC K278 · Atto-priority)

- **Agent Identity Card (AIC)** — four-dimensional identity binding (developer × code package × operator × deployment context), each dimension independently signed; one compromised dimension can't impersonate the rest.
- **Capability-aware discovery**: skill/tool advertisements are DID-bound Verifiable Credential manifests, verified for issuer provenance, subject binding, permission alignment, freshness before interaction.
- **Monotonic capability attenuation** + two-tier access control — least privilege is a signed structural invariant; application-level policy stays independent.
- **Kernel-mediated cryptographic audit trails** bound usage/delegation/execution to identity without a consensus ledger.
- No public SPDX clone (positioning paper) — **policy only**. Atto: apply AIC-style identity + capability-bound discovery + per-extract audit trails to solver callers.

## MARC-v1 clinical multi-agent (CCC K279 · GO REFERENCE)

- **Decomposer-from-NL** — generate the agent pipeline (roles, prompts, RAG config) from a plain-language task description; eliminate manual prompt engineering.
- **Stage-wise failure attribution** — traceable intermediate outputs per role-specialized agent; attribute failures to the specific stage, not the pipeline.
- **Declarative YAML agent config** — swap model/prompt/knowledge per agent with no code change.
- `.local/adopts/MARC-v1` (MIT) = structural REFERENCE for harness pipelines. **Clinical `wont_wire`** — no clinical runtime wiring.

## Repository-level verified code+proof eval (Vero / CCC K280)

- **Joint impl+proof eval** — grade implementation+spec-proof together; unit tests + human review cannot rule out the bug classes formal verification captures.
- **Unsat / incorrectness audit** — agents may prove the provided spec unsatisfiable or reference code incorrect; an eval must survive being told it's wrong.
- **Repository scale is the unit** — multi-module coherence, not single-function; frontiers solve 27/43 and close nothing on the hardest repos.
- `.local/adopts/vero` (Apache-2.0) = GO REFERENCE. Runtime **`wont_wire`** (Lean toolchain bench).
- **Dual-ID note**: today's meta-harness brief board **K237** also cites Vero (2608.13522) — resolve by arXiv id / brief path; this section's CCC **K280** remains the wire id.

## Meta-harness optimization with HITL / external eval contract (AutoDesign / CCC K281)

- **Meta-harness loop over the harness, not the model** — improve the harness from rollout feedback; harness gains transfer across model configs (+12.4% avg in K281).
- **External eval contract / HITL is the guardrail (pairs K162)** — pass criteria must NOT be closed self-rewritten by the optimizer; keep human design priors and external scoring authoritative.
- `[NEEDS VERIFICATION 2026-08-14]` — paper reports the external contract; whether the shipped harness enforces it at runtime is not audited in the paper.
- `.local/adopts/AutoDesign` (MIT on disk; gh API NOASSERTION but LICENSE file is MIT) = GO REFERENCE. Poster/Image-gen runtime **`wont_wire`**.
- **Dual-ID note**: today's meta-harness brief board **K237** also cites AutoDesign (2608.13560) — resolve by arXiv id / brief path; this section's CCC **K281** remains the wire id.

## CCC wave K282–K284 (shared policy file)

Recoverable execution (K282) + test-time world-model validate-before-act (K283) + off-nominal differential fault injection (K284). Local REFERENCE clones only (gitignored `.local/adopts/`); no CCC runtime wiring.

## Recoverable agent execution checkpoints (AgentRewind / CCC K282)

- **Aligned checkpoint = context + environment.** Record the pair `(context, environment)` at each LLM decision boundary, not either alone. Environment rewind is the largest single component (ablation: 87.8 → 43.9 success without it) — discarded-suffix side effects otherwise persist.
- **Resume-from-checkpoint, not restart-from-zero.** Restart-with-experiences discards completed work; rewind retains the validated prefix and replaces only the failing suffix.
- **Rewind memory** — retain prior-attempt info (falsified hypotheses, alternative strategies) with the restored context, so the second attempt is informed, not blind.
- **Recovery boundary honesty** — only the workspace directory tree is reversible; network/external-service side effects are not undone and must not be re-triggered (replay the retained prefix from the log, never re-execute).
- Recovery after error propagation — not only plan refinement pre-error (pairs K239 execution fidelity, K242 rollback).
- `.local/adopts/replay-agent-recorder` (MIT) + `.local/adopts/MettleBench` (Apache-2.0) = GO REFERENCE. Framework runtime **`wont_wire`**.

## Test-time world model — validate-before-act (Twin / CCC K283)

- **Validate-before-act is a harness gate, not agent discretion.** Block every scored/env action until the twin replays the full interaction log; a mismatch is a counterexample that invalidates the twin until repair.
- **Counterexample-driven repair (CEGIS)** — a repaired twin must replay the *full* log, so a local fix can't break an earlier mechanic.
- **Goal hypothesis before first reward** — propose a tentative goal predicate (must evaluate false on every logged frame; previously-reached non-goals are permanently excluded) and test the cheapest plan; a level boundary confirms.
- **Asymmetric objective** — fit (`∀` every logged transition) is the hard precondition; reachability (`∃` one imagined route) is pursued only through search.
- Pairs K231 Tycho — don't auto-build world models every verify-fail unless advantage-gated, but when building, require replay validation before any scored action.
- `.local/adopts/TWIN-ARC-AGI-3` (MIT) = GO REFERENCE. ARC bench runtime **`wont_wire`**.

## Differential fault injection for LLM modernization (CCC K284)

- **Off-nominal paired validation, not nominal alone** — a passing test suite doesn't exercise fault/perturbation/precision response; LLM code modernization needs paired differential fault injection at shared driver sites.
- **Differential compare original vs modernized at shared injection sites** — isolate the converted kernel as the only varying component; paired comparison (same fault → same classification/iteration/energy) is the acceptance criterion.
- **Measurement-guided hardening loop with explicit acceptance criteria** — human defines the fault model + criteria; the agent proposes; the campaign re-measures (no new SDC/CRASH/HANG, fewer target-mechanism outcomes, no extra absorption cost).
- **Match harness + compilation config** — a one-iteration skew was traced to -O1 vs -O2, not the code; compare per-run provenance logs.
- Off-nominal fidelity ≠ absolute resilience (reference behavior not assumed desirable).
- No public `ftinj` SPDX clone → **policy only**; scientific/GAMESS domain — no runtime on CCC laptop.

## OSINT wave K237 (shared policy file — dual-ID note)

CCC **ACCEL** is also labeled K237 (agentic-engineer competencies, above). Numbers below are the **OSINT 2026-08-14 ingest** (ente E2EE vault, CyberScraper, skill misevolution, HARD defense). Same-day Cybersec used ente as **K281**. Resolve by file+wiki, not bare K#.

## E2EE vault + zero-knowledge share (ente / K237)

- **Client-side encryption before upload; server stores ciphertext only.** Zero-knowledge share links (recipient URL + out-of-band key) + Shamir emergency recovery for client dossiers.
- **AGPL isolate (hard rule):** ente is AGPL-3.0 (721MB > clone cap — README/docs only). **No AGPL library links into proprietary Atto distribution kits**; clean-room pattern extraction only. Same for rachoon (AGPL Gotenberg billing).
- **Never put private Barone pedigree / living-person vitals in Atto fixtures or git** — Desktop/family only.
- `.local/adopts/` has **no ente clone** (size-skip); CyberScraper-2077 / rachoon / RustScan / vero reference clones only.

## CyberScraper GO cyber-lab (CyberScraper-2077 / K237)

- **Authorized-target-only scraping** — local-lab eval, whitehat perimeter audits, newsletter/substack polling the operator has rights to. No mass scraping.
- **Headless fallback required** for unattended cron runs (stealth mode attaches to a live Chrome via CDP/Patchright on localhost:9222).
- Playwright memory-leak watch during multi-hour polling loops.

## Skill misevolution SHE gate (2608.12851 / K237)

- **Skills can get worse with practice** — promote/refine skills under an eval-gate with decoupled benign-utility + harm judges, clean-session reload, No-Evolution baseline.
- **Govern at three lifecycle gates** (authoring / retrieval / execution) — a terminal success rate cannot distinguish a clean library from an unsafe skill never retrieved.
- **HITL on a skill's write does not cover retrieval-time harm** — need retrieval lineage + retirement on evidenced harm (2 harmful reuses), delete-only repair critic, library cap.
- Applies to `.cursor/skills/*` and CCC `/route` skill library.

## Self-evolving defense guardrails (HARD / K237)

- Defense evolution loop (attack → judge → route to policy/gate evolver → redeploy → held-out retest) is a **pattern for the cyber lab**, not a wired product.
- **Literal gate rules rot** (memorize attacks, generalize poorly) — prefer anti-obfuscation/cumulative-intent abstractions; scope invariants to preserve benign utility (never blanket-refuse a capability).
- **Over-restriction is a failure mode** — optimize ASR *and* UA jointly.
- RustScan is **GPL-3.0 (docx claimed MIT — wrong)**; Extract pattern only, batch-size discipline `-b 10 -T 5000` on sensitive targets.

## OSINT K239 — SteerBench action-boundary HITL · intent-not-form / HiRoute (2026-08-15)

**Dual-ID:** this section is **OSINT K239** (SteerBench-Work 2608.12654 + WIFA/HiRoute + eval). **CCC K239** is Datumpont / execution-fidelity (section above). **Capital Flows / GW K239** = GuruWatcher bond-crash product surface (`guruwatcher-k239-bond-crash-phase1-wires.mdc`) — a third K239; do not collapse. Resolve by file+wiki, not bare K#. Federation sync: keep this block so OSINT copies are not wiped.

- **Proceed vs hold at the action boundary** — state-changing tool calls (`scp` to prod, Discord LIVE, `watches.json` writes, skill mutation, AGPL/GPL vendor into Atto) default **HOLD for HITL**. Read-only wiki/query may proceed. Capability of the model is **not** a reason to drop the gate (SteerBench: over-refusal ≫ under-refusal; evidence-reversed mirrors fail).
- **Intent, not form** — refuse/allow on intent + tool effect + authorization, not wrapper phrasing. Do not train WIFA/HiRoute; **no jailbreak PoC** in wiki. Authorized owned-lab recon ≠ public exploit writeup.
- **No unattended auto-evolve** of `.cursor/skills/*`. Worker ≠ verifier (Osmani). `/goal` evaluator checks named metrics only — it is not taste.
- **GO Integrate this batch: none.** fclones MIT = Extract (Atto vault CoW; no unsupervised `--delete`). openai/privacy-filter Apache-2.0 = Extract (PII strip; ≠ packyme). rachoon stays **AGPL Extract-only**; RustScan stays **GPL-3.0 Extract-only** (docx Integrate + MIT were wrong). LFM encoders = license `other` + weight skip. timesfm code cloned; **no HF checkpoints**; GuruWatcher remains alert-only.
- **0day-Rubbish / LEAKSFORUMS** = Context only. No exploit payloads, no criminal-forum interaction.

## CCC wave K285–K289 (shared policy file)

Signed mandates on MCP actions (K285) + 3D world agents WATCH (K286) + black-box RL on opaque harness (K287) + planner-state semantic integrity (K288) + agentified eval evidence trees (K289). **No local clones** this batch (all null SPDX).

## Signed mandate chain at MCP proxy (Mandato / CCC K285)

- **Authorization is a signed artifact, not application code.** A mandate binds principal × agent workload identity × tool scope × parameter constraints × contextual conditions × TTL × delegation policy, signed by the principal (JAdES/JWS). Signing a mandate is a legal act — legible to auditors/lawyers.
- **Enforce at a non-bypassable MCP proxy.** Terminate both sides; upstream servers accept only the proxy's mTLS workload identity. Completeness + non-bypassability are architectural, not contractual.
- **Separated PDP/PEP, fail-closed.** Minimal in-line enforcement; decision point evaluates against mandate store + revocation view + quota counters. Escalations park via MCP task semantics → human console showing the *mandate clause* (AI Act Art. 14 "meaningful" oversight).
- **Closed constraint language.** Equality / enumerated sets / numeric ranges / string prefixes / presence-absence — one-line-per-predicate explainability; the policy must not become an unauditable program.
- **Monotone attenuation + deny-by-default.** A sub-mandate can only narrow scope / widen conditions / shrink validity, never weaken oversight (confirm cannot become auto downstream). Absence of mandate / expired chain / unverifiable signature / unknown tool / out-of-language constraint → DENY.
- **Append-only hash-chained audit.** Every permit/deny chained; selective hash commitments (GDPR minimization); Merkle checkpoints under RFC 3161 → qualified timestamps (eIDAS). Ratification corrects by appending, never editing.
- Pairs K278 InterSAGE (identity/VC trust), K271 MCP auth gateway (transport), K239 execution fidelity (deny-by-default). No public SPDX clone → **policy only**. **Atto-priority.**

## 3D world agents — rubric + feasibility verifier (VibeWorlding / CCC K286)

- **Unify fragmented 3D tooling under one MCP sandbox** (asset retrieve/add/rotate/translate/delete) — agent reasons over one interface.
- **Dual-constraint verifier as backbone** — physical feasibility (collision, height) AND intent fulfillment (ecological plausibility, retrieval). Handcrafted rules or a generic LLM judge alone are insufficient.
- **Same environment + reward for eval and training** — the rubric verifier is both benchmark scorer and RL reward service (no train/eval drift).
- `usail-hkust/VibeWorlding-Gym` null SPDX → **NO-GO clone**. **Reference only**; **`wont_wire`** CCC/Image-gen/3D. Game-dev stub.

## Black-box RL on opaque harness (ClawGym II / CCC K287)

- **Treat the harness as an opaque rollout engine** — don't instrument internals; intercept at the model boundary with a **serving proxy**.
- **Sandbox per rollout** — each task environment + harness in a temporary sandbox, provisioned on demand, destroyed on completion.
- **Rebuild prefix trees from captured calls** — fragmented/forked/redundant traces → prefix tree; adapt PPO (critic) and GRPO (critic-free) over the recovered tree.
- **Preserve training–inference consistency** — black-box token-in-token-out + token-level importance-sampling rollout correction, or training destabilizes.
- **Mix-harness joint training** — each task–harness pair is a training instance; rollouts from heterogeneous harnesses (OpenClaw + Claude Code) jointly optimize one model.
- Pairs K281 AutoDesign (optimize harness, model fixed — complementary axis), K262 scaffold>interface, K263 versioned model-harness pairs. No public SPDX repo → **policy only**.

## Planner-state semantic integrity (ESTI / CCC K288)

- **Planner-visible state producers are an integrity boundary** — compromise of exactly one can steer planning (objects, relations, affordances, task-stage rules, feedback).
- **Schema-preserving false evidence beats explicit commands** — false records conform to the field's semantic role/format; audit *consistency across records*, not just injected instructions.
- **Distinguish planning adoption (P-ASR) from execution realization (E-ASR)** — planning deviation ≠ physical attack success; measure both.
- **Defend carrier/representation consistency, not re-grounding** — removing runtime re-grounding moves P-ASR/E-ASR only +1.92/+3.85 pts.
- **Conditional-delivery scoping** — results are conditional on successful state delivery; don't overclaim write-access probability (pairs K259 traceable scholarship). Pairs K274 CDH trajectory audit. No public code → **policy only**; **Cybersec-primary**.

## Agentified eval — evidence tree not scalar (HarnessEval-W / CCC K289)

- **Eval = evidence tree, not a scalar** — deliver what was tested, which tool supplied the grounding, and the logical chain justifying the score.
- **Route case → skills → sub-agents with tools** — no fixed rubric fits all cases; decompose into measurable subproblems delegated to tool-equipped sub-agents.
- **Parent validates before verdict** — a verification gate between evidence collection and scoring (pairs K162: no closed self-eval).
- **Agentic, validated case construction** — sample scene taxonomy → ground actions → validate each candidate before acceptance (pairs K277 measurement integrity).
- `mirros-lab/harnesseval-w` null SPDX → **NO-GO clone**. **`policy_wired`**.

## CCC wave K290–K294 (shared policy file)

Least-privilege learning (K290) + enterprise BI WATCH (K291) + HCL (K292) + Eureka meta-agent (K293) + SPADE self-play envs (K294). Clone: SPADE MIT REFERENCE only.

## Task-conditioned least-privilege learning (K290)

- **Learned restraint complements gates** — post-training reduces excess-authority proposals; does **not** replace permission gates, signed mandates (K285), or sandboxing (K239).
- **Task-relative sufficient-authority envelopes** — define per-task minimum authority; penalize/score trajectories for excess beyond envelope.
- **Brokered pre/post audit** — deterministic six-dimensional risk vector on each terminal/MCP action; trajectory authority, not single-call classification.
- No public code → **policy_wired** only. **Atto-priority** (pairs K285). Cybersec brief for excess-authority eval axis.

## Enterprise conversational multi-agent analytics (K291)

- **Sequential specialized pipeline** reduces drift vs flat multi-agent chat — reference pattern for role boundaries (pairs K276 RSM).
- Enterprise Rakuten case study — **wont_wire** CCC/CeminiSuite runtime; no product install.

## Harness Continual Learning / HCL (K292)

- **Learning object = harness state** around a **frozen** foundation model (prompts, memory, skills, routing) — not only weights.
- **Harness-level forgetting** — coupled harness updates can break earlier behavior; measure retention on held-out packs.
- **Guarded evolution** — Optimizer proposes; Evaluator commits only after current improvement + historical retention budget + validity (pairs K162 external eval contract).
- Version harness baselines before blast-radius edits (pairs K273 ACM). No public code → **policy_wired**.

## Eureka task-conditioned meta-agent (K293)

- **Obligation graphs** with explicit acceptance semantics for long-horizon tasks — compile dynamic Macro-Agent topology at runtime.
- **Acceptance certificates** required before terminal success — no uncertified `done` (pairs K277 measurement integrity, K162).
- **Governed architecture evolution** — cost-benefit-gated local architecture updates under constraints.
- `manxis-contact/Eureka` null SPDX → **NO-GO clone**. **Policy only**.

## SPADE self-play executable environments (K294)

- **Co-evolving env designer** writes complete Gym-style Python envs (reset/step, rewards, verification) — env pool grows with agent capability.
- **Hint-based regret** rewards frontier-difficulty solvable envs — not pure adversary or cooperative inflation.
- Corpus grounding + environment memory for designer stability.
- `spade-rl/spade` **MIT** → **GO REFERENCE** `.local/adopts/spade`; RL trainer **`wont_wire`** on prod harness (pairs K162 closed-loop guardrails).

## CCC wave K295–K299 (shared policy file)

Hybrid HAI delegation (K295) + Thinkingbox terminal-state eval (K296, MIT REFERENCE clone) + RL-Trotter OOD (K297) + Task-CoEvolve adaptive val-task selection (K298) + MidTool mid-training tool-use WATCH (K299). Clone: thinkingbox MIT REFERENCE only. **CCC-only rule** `ccc-k295-k299-phase1-wires.mdc` — do **not** federation-sync.

## Hybrid HAI delegation — effort vs duration, person not task (CCC K295)

- **Measure interaction effort and wall-clock duration separately.** MCP+GUI hybrid cut clicks/page-nav/scrolls (N=73) but not completion time — do not claim speedups from tool/assistant presence; report steps eliminated per delegation as the real signal.
- **Delegation variance is person > task (ICC=.50).** Do not assume task risk drives delegation — users did not systematically avoid higher-risk CRUD; hybrid availability ≠ high-risk auto-delegate. Place confirmation/preview/reversibility **on the agent path** (pairs K239 execution fidelity).
- Design for the **single handoff** (state intent → confirm result), not sustained dialogue; do not make delegation the default.

## Terminal-state eval — one success ≠ reliability (Thinkingbox / CCC K296)

- For stateful, multi-turn, consequence-bearing harness evals: grade **terminal backend state + side effects + dialogue** with executable checks (wrong/missing/extra effects fail), not surface form or tool-call validity (pairs K277 labels≠endpoints).
- Report pass@1 **and** repeat-rate (pass^20): 65.36% pass@1 vs 25.25% pass^20 on 507 stateful business workflows — a single green run proves discoverability, not dependability.
- **Isolated sessions per attempt** (no shared DB rows/cached tool state) or pass@k and training rewards are unreliable.
- `.local/adopts/thinkingbox` (MIT) = GO REFERENCE; benchmark runtime **`wont_wire`**.

## RL-Trotter quantum simulation (CCC K297 — OOD)

- Physics OOD — source stub only; **`wont_wire`**; no Atto/3D/prod wire. Brief-over-stub.

## Adaptive validation-task selection (Task-CoEvolve / CCC K298)

- When iterating a harness optimizer loop: **sample informative val tasks** (disagreement/frontier, variance-weighted) and **estimate full-set scores from partial evals** (inclusion-probability weighting) — ~80% fewer evals, matches full-set on Terminal-Bench 2.1; a 20% budget can beat full-set (less overfitting).
- **Do not** auto-evolve `.cursor/skills` (misevolution) and **do not** rewrite `## Verify` / pass criteria mid-run (pairs K281 AutoDesign, K292 HCL, K162 external eval contract).
- `Agent4Science-UTokyo/Task-CoEvolve` **NO-GO clone** (no LICENSE). **Dual-ID: CCC K298** — fills the Task-CoEvolve item in the k244 steal brief; K244 stays UrbanAgent.

## Mid-training for agentic tool use (MidTool / CCC K299 — WATCH)

- **Model-choice awareness:** dedicated tool-use mid-training (affordance recognition, argument grounding, workflow composition, incomplete-info recovery) beats leaving tool use to post-training — prefer families with agentic mid-training for MCP-heavy lanes.
- Evaluate the four atomic tool-use capabilities, not raw function-call accuracy (pairs K259 parameter grounding).
- **`wont_wire`** runtime; **no HF `MidTool/midtool-release` download**.

## Skill-set budget (BPS) + wrap-don't-rebuild (EnvHarness) + Wayfinder

Steal-from k244 cluster (arXiv 2608.19993 / 2608.19880 + Latent Space wayfinder). **Not** CCC K244 (UrbanAgent). Skills: `.cursor/skills/{skill-set-budget,env-harness-wrap,wayfinder}/` (`federation: true`, operator-invoked). No paper-algorithm clone; no EnvRigger auto-plug-ins.

- **Set, not top-k (BPS).** When several skills could apply, add only complements under a token budget; skip distractors. Do not pack by independent semantic rank (paper: Best Prefix Selection).
- **Wrap, don't rebuild (EnvHarness).** Keep the existing verifier (`wiki_lint`, OpenSpec, route `## Verify`, tests). Adapt via wrappers; HITL before changing the scorer. EnvRigger-style trajectory→plug-in synthesis is **WATCH** — no unattended skill/harness mutation (pairs K162 / K281 / K298).
- **Wayfinder** is opt-in fog-of-war planning (map / tickets / session). Bounded one-session paths still use `grill-with-docs`.

## Phase-1 meta

- After ingest ADOPT/GO: propose `/phase1` wire before claiming done.
- Inventory: `python3 scripts/phase1_adopt_inventory.py --write-brief`
- Skip Image-gen / 3D local wire backlogs.
- **TipDrop/David kit retired 2026-08-05** — lab briefs → `Cybersecurity wiki/briefs/` (local-abliterated-lab), never tipdrop-workspace-kit.

## CCC wave K300–K309 (shared policy file)

AutoMOOSE OOD stub (K300) + Graph Engineering vocabulary (K301) + AID-Guard effect closure (K302) + health CUI trust OOD stub (K303) + memory-poisoning bounded occupancy (K304) + MCP-Universe RL WATCH trainer (K305, Apache-2.0 REFERENCE clone) + ARGUS K8s RCA asymmetry (K306) + MediSkill-Evo process-constrained evolution pattern (K307) + MetaCaster intermediary-engineer TSF (K308, MIT REFERENCE clone) + Prime Agent RLM/Continual Harness (K309, MIT REFERENCE clone <500MB). Clones: MCP-Universe, metacaster, prime-agent — REFERENCE only. **CCC-only rule** `ccc-k300-k309-phase1-wires.mdc` — do **not** federation-sync. **Dual-ID:** same-day Cybersec K301–K306 = CLEAR / PsychJail / CLAUDE.md-deny / SDP-RIM / BT-NFT / LLM-compliance; CCC K253 Argus persist/pivot ≠ CCC K306 ARGUS RCA; CCC K229 pi-rlm ≠ CCC K309 Prime Agent.

## Authorization-to-effect closure (AID-Guard / CCC K302)

- **Admission approval ≠ effect authority.** Revalidate the exact approved request and current provider state at commit; a request that changed before commit is unauthorized by default.
- **One reservation while ambiguous** — unknown outcome ⇒ hold; never blind-retry into a duplicate effect.
- **At most one successor**, only after terminal result or certified no-effect with a delivery fence across the declared recovery horizon.
- Paper evidence: 13 live mutations clean; 210/210 Stripe trials matched; 44/44 compromise attacks blocked at 35.4–43.8 pp strict-profile utility cost. Pairs K239 execution fidelity + K285 Mandato. No public repo → **policy_wired** only. **Atto-priority.**

## Memory poisoning — bounded occupancy + write-path grounding (CCC K304)

- **Content screening cannot see payload-free lies**: 0.832 injection recall yet 0/360 plain false assertions refused; 1.2% poison → LongMemEval 0.850→0.300.
- **Additive provenance ranking has no usable weight** (shipped weight p=0.80 vs none; higher weights exclude untrusted categorically). Use provenance as a **bounded occupancy constraint** on retrieved context instead.
- **External grounding required before persistent writes** — truth is not a text property. No poison PoCs in wiki or briefs. Pairs K230 reconstructive memory. Cybersec dual-ID K304 = SDP-RIM.

## Diagnostic/prescriptive asymmetry — never auto-apply remediations (CCC K306)

- ARGUS (MCP-grounded K8s RCA over Prometheus/Loki/NATS): correct root cause **10/10**, MCP success **0.91**, but engineers consistently distrust prescribed fixes.
- Wire agents as diagnostic engines with evidence attached; keep remediation human-gated everywhere (alerts, autofixes, harness self-repair). Do not merge with CCC K253 Argus persist/pivot (`lbx154/Argus`) — different product. Runtime **`wont_wire`**.

## Process-constrained self-evolution (MediSkill-Evo / CCC K307)

- **Typed banks** (skills / rules / schemas / measurement) with per-type safeguards — not one retrieval bag.
- **Frozen test-time snapshot published before evolution runs**; candidates scored against what they cannot influence (pairs K162 external-eval contract).
- **Process critic with veto** rejects controller-invalid candidates outright. Clinical domain `wont_wire`; never evolve `.cursor/skills` unattended (pairs K237).

## Intermediary-engineer agents (MetaCaster / CCC K308)

- For high-frequency cheap-decision surfaces: the agent **builds/maintains the small specialist** (train from few examples + text); it does not serve each prediction itself.
- Meta-harness may optimize harness (prompts/skills/tools) — **never the pass criteria** (pairs K281/K162). MIT REFERENCE clone `.local/adopts/metacaster`; TSF runtime **`wont_wire`**; GuruWatcher pointer alert-only.

## RLM REPL + Continual Harness (Prime Agent / CCC K309)

- Context as programmable data in a persistent REPL; histories/memories/skills/prompts/subagent specs persisting across trajectories; recursive subagents + human-inspectable Agents View.
- Standardized execution/recovery/verification keeps **harness failures from scoring as model failures** (ARC-AGI-3 RHAE Best@1 30%→95.5%, paper claim).
- Guardrails: **HITL before any skill/prompt self-write**; retention checks on held-out packs before trusting learned state (K292); **do not replace Cursor / `/route` / claude-ds** — runtime `wont_wire`, clone REFERENCE only (pairs K229 pi-rlm brief).

## AID-Guard commit + Prime Agent sandbox + intermediary-engineer (HITL 2026-08-25)

Operator-invoked federation skills (`disable-model-invocation: true`): `.cursor/skills/{aid-guard-commit,prime-agent-sandbox,intermediary-engineer}/`. No paper-product install; no `curl|bash`.

- **AID-Guard helper** — `scripts/aid_guard_commit.py` reserve → commit (exact hash) → release / certify-no-effect / one successor. Default demo path is `local.hot_md`. GitHub MCP file writes and stash writes use the same protocol; **preview/HITL still required** (K239). `python3 scripts/aid_guard_commit.py selftest` is the executable check. Not a Stripe/LIVE proxy.
- **Prime Agent sandbox** — `scripts/prime_agent_sandbox_trial.sh` is inventory + K292 retention (harness files unchanged). **Never** run upstream `install.sh`; **never** PATH-swap `/route`.
- **Intermediary-engineer** — high-frequency numeric surfaces: agent builds the specialist, does not serve each prediction. GuruWatcher: `guruwatcher-k308-metacaster-phase1-wires.mdc` alert-only; no TSF runtime.

## CCC wave K310–K314 (shared policy file)

AP2 pre-auth intent (K310, policy) + SCOUT lazy MCP catalog (K311, policy) + StepGuard step-level guardrails (K312, policy) + StarHarness stratified evolution (K313, MIT REFERENCE clone) + Recuris EM–WM memory coupling (K314, Apache-2.0 REFERENCE clone). Clones: StarHarness (24MB), Recuris (4MB) — REFERENCE only. **CCC-only rule** `ccc-k310-k314-phase1-wires.mdc` — do **not** federation-sync. **Dual-ID:** CCC K285 Mandato ≠ CCC K310 pre-authorization context; same-day Cybersec AP2/StepGuard ingests resolve by file + wiki.

## Mandate signature vs pre-authorization intent (CCC K310)

- **A signed mandate proves integrity of bytes, not user intent.** A2A messages and MCP tool results that shape a transaction stay unsigned; manipulate them and a valid signature encodes an unintended action (48 threats / 8 High by AIVSS across five deployment architectures).
- Revalidate the approved request **and** the context it was approved against at effect time (pairs K302 revalidate-at-commit). Treat unsigned shaping inputs as attacker-controllable in every threat model.
- No exploit text or PoCs in wiki/briefs; no paper testbed/scanner clone. `policy_wired`.

## Lazy hybrid MCP tool discovery (CCC K311)

- Never load the full tool catalog at turn 0. Retrieve per step (`tool_search`: BM25 + dense fused by RRF, top-k≈5) then execute (`execute_tool`). PayPal production: 140.2k → 1.3k tool tokens (99%) with zero client changes.
- Prompt caching does not fix this — the model still attends to the whole catalog. Pairs BPS set-not-top-k + K164 lazy skill stacks.
- No public repo → `policy_wired`. SCOUT meta-tools **shipped 2026-08-28 as operator-invoked skill** `scout-tool-search` (federation, `disable-model-invocation: true`) — local SKILL.md frontmatter top-k search, not a `/route` rewrite. **BM25-lite 2026-08-31**: `scripts/scout_tool_search.py` k1=1.5, b=0.75, IDF from local corpus — no pip/HF/vector DB.

## Gate the step, not only the answer (CCC K312)

- Check candidate tool actions **before execution**; audit completed trajectories afterwards. Guards are tuned on an explicit safety–utility dial (StepGuard: ASR −77.3%, utility −2.8 pts) — over-defense burns utility, under-defense burns safety.
- Context-matched supervision (safe/unsafe variants diverge only at the risky step) stops guards from keying on tool identity. Pairs K239 preview-before-effect.
- `zheng977/StepGuard` license:null → NO-GO clone; trainer `wont_wire`, policy `policy_wired`. Step gate **shipped 2026-08-28 as operator-invoked skill** `step-gate` (federation, `disable-model-invocation: true`) — closed proceed/hold/escalate classifier. **Real PreToolUse hook 2026-08-31**: `scripts/claude_pretooluse_step_gate.py` in `~/.claude/settings.local.json` (matcher `Bash|Write|Edit`, timeout 5s) — **denies only HOLD**, PROCEED/ESCALATE allow, `STEP_GATE_HOOK=0` kill switch; no Stop hook, no deny-on-ESCALATE.

## Stratified harness evolution (CCC K313)

- Evolve harness (prompts/tools/skills/MCP/subagents/loop) with **weights frozen**; pool stratified by baseline failure mode; proposer-visible search tasks ≠ proposer-hidden selection tasks ≠ held-out eval; accept few changes (4–12 → +20–35 pp); transfer cross-model without re-evolution.
- Never optimize pass criteria; no closed rewrite of `## Verify` (pairs K162/K281/K292/K298). Do not auto-evolve `.cursor/skills`.
- `ServiceNow/StarHarness` MIT → GO REFERENCE `.local/adopts/StarHarness`; trainer `wont_wire`.

## EM–WM coupling + validation-gated skill memory (CCC K314)

- Working Memory tracks live task state and **picks** skills from Experiential Memory — grounding skill use in current needs instead of full history (+17.8 pp τ²-Bench GPT-5.6 Sol; failure modes −20–86%).
- Execution traces localize failures to a specific memory component; Meta-Agent applies **validation-gated** patches; loop stays bounded. **HITL before any skill self-write** (pairs K237/K298/K307/K309).
- `Gen-Verse/Recuris` Apache-2.0 → GO REFERENCE `.local/adopts/Recuris`; runtime `wont_wire`.

## CCC wave K315–K324 (shared policy file)

ToolMinimize arg rewriting (K315, policy) + LifePlanner OOD (K316) + TAU-Agent OOD (K317) + ProgRouter step-wise routing (K318, policy) + AsymSpec serving WATCH (K319) + Claude Code Handbook delta (K320, reference) + ASIL structured state (K321, policy) + MoRe role fusion WATCH (K322) + INTENT-AS-A-TOOL (K323, MIT REFERENCE clone) + RedEvoAgent validation ratchet (K324, Cybersec-primary steal). Clone: intent-as-a-tool (17MB) — REFERENCE only. **CCC-only rule** `ccc-k315-k324-phase1-wires.mdc` — do **not** federation-sync. **Dual-ID:** CCC K324 RedEvoAgent = Cybersec-primary same-day PDF; CCC K285 Mandato ≠ K315 ToolMinimize.

## Minimize tool-argument values, not only allow the call (CCC K315)

- Allow/block and information-flow controls decide *whether* a call happens; they never rewrite *what data* the tool receives. 81–88% of tool calls over-share PSD under default prompts; privacy prompts leave 36–76%; Llama barely responds (81%→76%).
- Middleware rewrite with schema-aware necessity + four operations (removal / generalization / substitution / truncation) cuts privacy cost 81.2–92.0% at **100% argument-level task validity** (1.77 ms median). PII detectors miss implicit PSD (a hospital name implies a diagnosis).
- No public SPDX → `policy_wired`. No PII/PoC dumps in wiki/briefs. Pairs K259/K239 allow/block gates.

## Incomplete evidence + imprecise tools, not model size (CCC K316, OOD)

- LifePlanner: Pass Rate drops to **40.2%** on complex geo-planning; failures stem from incomplete evidence acquisition, imprecise tool use, weak constraint integration — not model size or reasoning length.
- OOD trip-planning bench → short stub, `wont_wire`. Pairs K244/K246.

## Retrieve evidence first, then reason (CCC K317, OOD)

- TAU-Agent: a central retrieval agent selects query-relevant evidence (captions, temporal intervals, object trajectories) **then** the VLM reasons — lazy, not dump. OOD stub, `wont_wire`. Repo MIT but no clone.

## Route per step from progress + difficulty + cost (CCC K318)

- One-shot cascade routing picks an LLM per query and cannot adapt mid-workflow. Route **per step** with a multi-view progress scorer (outcome regimes + subtask completion + progress trends + state quality) + meta-gated progress-gain estimation under time/cost budgets.
- Reduces operating cost vs baselines on HumanEval Plus / MBPP / MATH-500 / ASQA. Pairs `/route` quality-cost. No public SPDX → `policy_wired`.

## Context-asymmetric speculative decoding (CCC K319, WATCH)

- Serving-only: drafter reads full context, verifier sees compressed; δ-fusion + divergence-aware acceptance. ≈90% full-context accuracy at 1.3–1.7× throughput / 0.2–0.3× compute. Huawei; `wont_wire`; no clone.

## Claude Code handbook delta (CCC K320, reference)

- Control stack: 4 layers, only 2–4 constrain (advisory instruction shapes intent; permission deny→ask→allow; sandbox; isolation). Hooks cut across 1–2 but never bypass a deny.
- Plan-as-contract (8-point minimum complete plan; approve the plan, not "any method"); measure `/usage` (allocation) vs `/context` (occupancy) separately; completion reports distinguish checks that ran from checks described; Evidence-Gated Delivery with human release gate; 21 misconception corrections (M01–M21) — e.g. `/loop` ≠ cron, workflow agents always run acceptEdits, green routine status ≠ task success. `[TENTATIVE]` vs Anthropic docs.

## Structured state + semantic actions over screenshot-click (CCC K321)

- Screenshots are a state-incomplete projection; GUI clicks are brittle and semantically weak. Expose JSON observations + code-executable semantic actions via the **deepest feasible access path** (API → CLI → service → accessibility → GUI).
- ASIL >80 at <5 actions/task vs 6.6/26.6 screenshot-click strict on matched tasks. Structured modality trains better too (SFT Qwen3.5-2B 58.0→72.1). No SPDX hunt hit → `policy_wired`; GUI runtime `wont_wire` — keep cursor-ide-browser, do not add CUA.

## Do not auto-spawn N agents for multi-perspective work (CCC K322, WATCH)

- MoRe fuses a codebook of role steering vectors per query into one composed vector on a frozen backbone: MAS-level quality at **20× lower token cost** (+2.2% over single-agent baselines). GRPO `wont_wire`; no clone.

## Intent-targeted tools = judge-free commitment signal (CCC K323)

- Tools are intent-agnostic; intent tools make commitment an observable action. Call probability is a fine-grained, judge-free signal that complements CoT monitoring, expands labels into dense trajectories, and pinpoints online-intervention steps.
- `RebeccaZhang22/intent-as-a-tool` MIT (17MB clone) → GO REFERENCE `.local/adopts/intent-as-a-tool`; runtime `wont_wire`. No attack/PoC text.

## Validation ratchet for skill updates (CCC K324)

- Keep a skill-memory patch only if it improves validation performance (RedEvoAgent's ratchet, taken defensively). Attribute updates to the tool that drove the outcome — never credit ambiguous/misleading experience.
- CCC applies the same gate to any skill-memory patch (pairs K237/K313/K314); never auto-evolve `.cursor/skills`. No attack-skill clone; Cybersec-primary.

## CCC wave K325–K327 (shared policy file)

Security-agent SLR bounded-authority gap (K325, Cybersec-primary steal) + Recognition Without Enforcement external monitor (K326, policy) + Logos cross-process plugin harness (K327, policy). **No clones** — no InstructionArbitrationBench (no SPDX, forthcoming), no Logos (no SPDX). **Zero attack text / no paper prompts / no PoCs anywhere.** CCC-only rule `ccc-k325-k327-phase1-wires.mdc` — do **not** federation-sync. **Dual-ID:** same-day Cybersec inbox holds 2608.28490 + 2608.28502 (its own K#); CCC K285 Mandato / K310 AP2 / K312 StepGate ≠ K326 external monitor.

## Bounded authority + auditable trajectories (CCC K325)

- Security agents are built to **act** (91% modular, 88% planner-and-actor, 95% fully autonomous) but rarely bounded or inspected (15% critic/verifier, 13% guardrail layers, 13% human review). Outcome metrics dominate (success 51%, P/R/F1 34%); process metrics are near-absent (hallucination 11%, scope containment 5%, tool-call accuracy 1%).
- CCC: keep **HITL on irreversible tools** (exploit execution, production remediation, disclosure, active response — pairs K239); evaluate with **evidence trees, not scalars** (pairs K289); require **evidence ledgers** per claim (source, time, producing tool/model, confidence, validation status — pairs K277). Survey: `wont_wire`.

## Recognition ≠ enforcement — external reference monitor (CCC K326)

- Model self-arbitration is a **capability, not a security boundary**. Models decode source-format markers (97.5–100%) and verbalize forged authority, yet still act on spoofed instructions under permissive configurations (one model detects 98.7% / executes 99.3%).
- Fleet-mean execution under novel spoofs is **1.21% [0.5–2.1%]** but **concentrated in reproducible prompt–model cells** (40–100% deterministic) and shifts **up to 47pp across deployment windows**; restrictive policy eliminates execution on the same weights. Prompt-layer defenses do not generalize.
- Enforcement must be **external and fail-closed**: authenticated source routing (HMAC-SHA256 per-record MACs, deterministic under EUF-CMA) + capability-gated tool execution (in-context advisory token + external execution-layer verification the model cannot bypass; single-use nonce-bound tokens). Rejects forged/tampered/replayed/unsigned by construction.
- Residual boundary is **semantic authorization**: authentication proves who signed, not whether content carries authority (confused deputy). Gate the tool execution layer, not the model's opinion. `policy_wired`; pairs K285 Mandato, K310 pre-auth intent, K312 StepGate. **No attack text; do not clone the forthcoming bench until SPDX.**

## Plugin = process, append-only transcript (CCC K327)

- One process = one failure domain; a single-process plugin host is a SPOF for every co-resident session. Isolate plugins as **peer processes** on a bus (router holds only the routing table); one fault ends at one node.
- Keep durable state in an **append-only transcript** owned by no process; recovery (**cold switching**) rebuilds the session from the transcript. Resume with **no repeated effect** at the four tool-call-cycle boundaries (during execution / after return before persistence / after persistence before announcement / after announcement) — 80/80 sessions clean; work redone = work the transcript had not recorded.
- CCC: isolate high-blast tool surfaces; do not re-execute a retained prefix (pairs K282 AgentRewind, K239 execution fidelity). No public SPDX → policy only, no clone; runtime `wont_wire` — do not replace Cursor/`/route`.

## CCC wave K328–K332 (shared policy file)

Output attestation + delegation ancestry (K328, policy) + Domain-grounded tool orchestration (K329, pattern) + CastClaw HITL forecasting (K330, OOD steal) + AutoSciRub evaluation-first rubric induction (K331, eval-first) + Aspire vague-goal self-evolution (K332, policy). **One clone:** `zjunlp/AutoSciRub` MIT (~2.5MB) → REFERENCE `.local/adopts/AutoSciRub` — runtime `wont_wire`. **CCC-only rule** `ccc-k328-k332-phase1-wires.mdc` — do **not** federation-sync. **Dual-ID:** Cybersec may later take 2608.30387 (attestation) or 2608.30083 (ZK agent proofs — cap-skipped this wave); CCC K285 Mandato / K278 InterSAGE / K310 AP2 remain distinct from K328 output attestation. **No PoCs / no attack templates anywhere.**

## Credentials ≠ later output bytes — two-layer attestation (CCC K328)

- Post-incident evidence questions, not behavior claims: which deployer released these exact bytes, and was each cross-deployer edge authorized? A credential records a principal + permission at issuance; it does **not** bind later output bytes (G1) or prove both deployers authorized a dynamically created edge (G2).
- **Two independently verifiable layers:** (1) a trusted **deployer runtime** (never the LLM agent) signs a hash of each released output — records released bytes; **does not prevent prompt injection**; (2) **ancestry evidence** binds each delegation record to a parent, and cross-deployer edges require **parent co-signature**.
- After **child-key compromise**, a signed linked list / Merkle-chain accept a forged parent binding (child-only signature); only a **co-signed DAG rejects it** (parent must authorize the edge; negligible forgery under SUF-CMA). Cost small: Signed-DAG +5.5% end-to-end vs no-evidence in a live MCP/A2A/LLM workflow; 1,000/1,000 AWS paths verified.
- Treat attestation as **post-incident audit/attribution**, never behavioral safety (pairs K285 Mandato integrity-not-intent, K310 pre-auth intent, K278 InterSAGE edge trust). **Cybersec steal (no PoCs).** `policy_wired`.

## Intent / execution / explanation split over MCP + scoped ontology facts (CCC K329)

- Script generation fails on **API hallucination, missing pipeline stages, wrong-strategy errors** (code that executes but answers the wrong question — domain-knowledge failures, not code-quality). Assign NL to the LLM, execution to **typed deterministic tools** that return structured data, explanation back to the LLM.
- A small domain ontology (~25–30 entries) constrains planning to valid analysis chains. **It does not change tool selection (null result — schemas suffice); it corrects result interpretation 0.41→0.91** — and **only when the relevant fact is retrieved scoped, not bulk** (dump 0.56 / mined 0.74 / scoped 0.91).
- Marginal cost of a new domain = ontology + tool wrappers; architecture/protocol unchanged. ParaView/TTK runtime **OOD** `wont_wire`; pattern `policy_wired`. Pairs K259 / K272 / K316.

## Forecasting is a harness task (CCC K330, OOD)

- Leave numerics to specialist forecasters; the harness connects models, tools, user input, and a **versioned execution record**. **Ask for missing essentials — no silent defaults.**
- A **revision replaces the active forecast only after validation + hard constraints**; HITL cannot bypass those gates. Record every revision and the stopping decision; stop when supported, low expected value, budget out, or expert needed.
- GuruWatcher: **alert-only pointer** to CastClaw — no new TSF runtime, no invented GW rule file. `wont_wire`.

## Evaluation-first rubric is the control surface (CCC K331)

- Open-ended research hides success criteria. **Induce a task-specific executable rubric before execution** and use it to guide, verify criterion-by-criterion, and revise — gains transfer across harnesses with a frozen backbone (+2.08/+2.95 pts ResearchClawBench; +16.8 AstaBench; rubric-guided ≈2.7× rubric-free self-refinement).
- **Keep the external eval contract; do not let the agent rewrite pass criteria — do not rewrite `## Verify`** (pairs K162 / K281 / K289 / K298 / K324 validation ratchet).
- `zjunlp/AutoSciRub` **MIT** (gh SPDX 2026-09-01) → GO REFERENCE `.local/adopts/AutoSciRub` (6MB). Entity runtime `wont_wire`; concept `policy_wired`.

## Vague-goal self-evolution ≠ explicit-task optimization (CCC K332)

- Goal interpretation is the **missing search axis**. With only a vague goal + hidden eval, agents close the train/edit loops but retain little: 30 → 28 evaluated → 21 eligible → **1 retained above-base improvement**; a positive within-lineage slope can be recovery from training-induced regression.
- **Local training success ≠ transferable capability** (proxy self-eval fails hidden eval: 8/8 on a self-built checklist, 19.32 vs 28.64 reference); **continued search can erase earlier gains** (20.10→2.78→18.17) — checkpoint selection + rollback required; **strongest evolved harness < engineered reference** (27.22 vs 28.64 Qwen-Agent). Judge progress against the **base model**, not the previous checkpoint.
- **Treat harness self-edit as high-blast HITL (misevolution)** — **never auto-evolve `.cursor/skills`** (pairs K237 / K292 / K313 / K324). No public SPDX → no clone; runtime `wont_wire`.
