---
description: CCC Phase-1 wires from K328–K332 harness wave (CCC-only — do NOT federation-sync)
alwaysApply: true
---

# CCC — K328–K332 Phase-1 wires

**Brief:** `docs/briefs/2026-09-01_k328-k332-harness-wave.md` · SIP: `wiki/briefs/2026-09-01_ccc-k328-k332-sip-ready.md`

**IDs:** Resolve by arXiv id / slug / file path — K# is a log batch label only.

- **K328** Attesting Outputs and Delegation Ancestry — **ADOPT policy**: credentials ≠ later output bytes. Two evidence layers: **deployer-runtime output-hash signature** (records released bytes; does **not** prevent prompt injection — output attestation ≠ behavioral safety) + **edge-authorization ancestry**. After **child-key compromise**, only a **co-signed DAG** rejects an unauthorized parent binding; a signed linked list / Merkle-chain accept it (child-only signature). Trust: runtime, signing key, admission-controlled registry, verifier — bounded scope. Pairs K278 InterSAGE / K285 Mandato / K310 pre-auth intent. **No public SPDX → policy only, no clone. Cybersec steal (no PoCs).** `policy_wired`.
- **K329** Domain-Grounded Tool Orchestration — **ADOPT pattern**: separate **intent** (LLM) / **execution** (deterministic domain tools) / **explanation** (LLM) over MCP; a small domain ontology constrains planning to valid analysis chains. Script generation fails with API hallucination, missing stages, wrong-strategy errors. The ontology does **not** change tool selection (null result — schemas suffice) but raises **result interpretation 0.41→0.91**, and **only when the relevant fact is retrieved scoped, not bulk** (dump 0.56 / mined 0.74 / scoped 0.91). Pairs K259 / K272 / K316. **ParaView/TTK runtime OOD `wont_wire`; no clone.** `policy_wired`.
- **K330** CastClaw — **OOD** TSF demo (4 pages), short stub. Steal: **leave numerics to specialist forecasters**; **ask for missing essentials (no silent defaults)**; a **revision replaces the active forecast only after validation + hard constraints** (HITL cannot bypass those gates); **versioned execution record**; stop when supported, low expected value, budget out, or expert needed. GuruWatcher: **alert-only pointer** — no new TSF runtime, no invented GW rule file. `wont_wire`. No clone.
- **K331** AutoSciRub — **ADOPT eval-first**: induce a task-specific **executable rubric before** research execution; use it to guide, verify, and revise (criterion-level). Gains transfer across harnesses with frozen backbone (+2.08 pts across 3 backbones under Codex; +2.95 pts across 3 harnesses under DeepSeek-V4-Flash; +16.8 pts AstaBench; rubric-guided ≈2.7× rubric-free self-refinement). Pairs K162 / K281 / K289 / K298 — **keep the external eval contract; do not rewrite `## Verify`**. `zjunlp/AutoSciRub` **MIT** (gh SPDX) ~2.5MB → **GO REFERENCE** `.local/adopts/AutoSciRub`. Concept `policy_wired`; entity runtime `wont_wire`.
- **K332** Aspire — **ADOPT policy**: vague-goal self-evolution ≠ explicit-task optimization. Agents complete train/harness-edit loops but weight gains are sparse (30→28→21→1 retained above-base cells); **strongest evolved harness stays below an engineered reference** (27.22 vs 28.64); **proxy self-eval fails hidden eval** (8/8 self-built checklist, 19.32 hidden); **continued search can erase earlier gains** (20.10→2.78→18.17); **goal interpretation is the missing search axis**. Pairs K162 / K237 misevolution / K292 / K313. **Never auto-evolve `.cursor/skills`; treat harness self-edit as high-blast HITL.** Project page only; no public SPDX → no clone. Runtime `wont_wire`.

**Wires landed:**

| K | Verdict | Wire | Entity/concept wire_status |
|---|---------|------|----------------------------|
| K328 | ADOPT policy | Deployer-runtime output signing; parent co-signature for cross-deployer edges | `policy_wired` (output-attestation-and-delegation-ancestry) |
| K329 | ADOPT pattern | Intent/execute/explain split; scoped ontology facts; new domain = ontology + wrappers | `policy_wired` (domain-grounded-tool-orchestration); runtime `wont_wire` |
| K330 | OOD steal | Forecast = harness task; validation-gated revision; versioned execution record; GuruWatcher alert-only | `wont_wire` (no concept/entity) |
| K331 | ADOPT eval-first | Evaluation-first rubric = control surface; keep external eval contract; no `## Verify` rewrite | concept `policy_wired`; entity `wont_wire` runtime (AutoSciRub REFERENCE clone) |
| K332 | ADOPT policy | Goal interpretation first; judge vs base model; harness self-edit = high-blast HITL; never auto-evolve `.cursor/skills` | `policy_wired` (vague-goal-self-evolution); runtime `wont_wire` |

**Leftovers / non-goals:** no federation sync of this file; no Aspire / ParaView / CastClaw clones; no HF weights; 2608.30083 (ZK agent proofs) not fetched this wave (cap-skipped); no `.cursor/skills` auto-evolution; no `route-task.ps1` rewrite; no `~/.claude/settings.json` edits; no PoCs / attack templates anywhere.
