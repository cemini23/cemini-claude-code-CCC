---
description: CCC Phase-1 wires from K310–K314 harness wave (CCC-only — do NOT federation-sync)
alwaysApply: true
---

# CCC — K310–K314 Phase-1 wires

**Brief:** `docs/briefs/2026-08-26_k310-k314-harness-wave.md` · SIP: `wiki/briefs/2026-08-26_ccc-k310-k314-sip-ready.md`

**Dual-ID (resolve by file + wiki, not bare K#):** **CCC K285** = Mandato signed-mandate enforcement (`@concepts/signed-mandate-mcp-protocol-enforcement.md`) ≠ **CCC K310** = pre-authorization context outside the signature. Same-day Cybersec may ingest AP2 / StepGuard — do not merge. Do not delete BPS / Wayfinder / AID-Guard leftover skills blocks in the shared policy file.

- **K310** AP2 Beyond the Mandate — **ADOPT policy**: valid mandate signatures ≠ user intent when pre-authorization context (A2A messages, MCP tool results) is manipulated. 48 threats / 8 High (AIVSS) across five architectures. Bind pre-commit context (pairs K302 AID-Guard revalidate-at-commit). **No PoCs or exploit text anywhere; no paper testbed/scanner clone** → `policy_wired`.
- **K311** SCOUT — **ADOPT lazy hybrid retrieval**: never load the full MCP tool catalog at turn 0; two meta-tools (`tool_search` → hybrid BM25+dense RRF top-k → `execute_tool`); 140.2k → 1.3k tokens (99%) at PayPal across six clients incl. Claude Code/Cursor. No public repo → `policy_wired`. Leftover propose-only: SCOUT-style meta-tools on `/route` (HITL later).
- **K312** StepGuard — **ADOPT policy**: check candidate tool actions **before execution** + audit completed trajectories; explicit safety–utility dial (ASR −77.3%, utility −2.8 pts). Gate the step, not only the final answer (pairs K239 preview, K264 accept/reject/escalate). `zheng977/StepGuard` license:null → NO-GO clone; trainer runtime **`wont_wire`**, policy `policy_wired`. Leftover propose-only: PreToolUse-style step gate (HITL later).
- **K313** StarHarness — **ADOPT stratified harness-evolution protocol**: freeze weights; stratify pool by baseline failure mode; proposer-visible search ≠ proposer-hidden selection ≠ held-out eval; accept few changes (4–12 → +20–35 pp); gains transfer cross-model without re-evolution. `ServiceNow/StarHarness` **MIT** <500MB → **GO REFERENCE** `.local/adopts/StarHarness`; trainer **`wont_wire`**. Do **not** auto-evolve `.cursor/skills` (pairs K281/K292/K298/K307); no closed rewrite of `## Verify`.
- **K314** Recuris — **ADOPT EM–WM coupling**: Working Memory tracks task state and picks skills from Experiential Memory; traces localize failures to memory components; Meta-Agent applies **validation-gated** Skill Memory patches (+17.8 pp τ²-Bench GPT-5.6 Sol; +32.2 pp longest horizon). `Gen-Verse/Recuris` **Apache-2.0** <500MB → **GO REFERENCE** `.local/adopts/Recuris`; runtime **`wont_wire`**. **HITL before any skill self-write** (pairs K309/K237).

**HITL leftovers:** none implemented this wave — SCOUT meta-tools on `/route` and StepGuard PreToolUse gate are propose-only. Clones are reading references only. Do **not** federation-sync this CCC-only file.
