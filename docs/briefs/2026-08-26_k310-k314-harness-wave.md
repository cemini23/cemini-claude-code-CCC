---
title: K310–K314 CCC — harness wave
type: brief
created: 2026-08-26
---
## Target
CCC
## Summary
AP2 pre-authorization intent ADOPT policy, no PoCs (K310) · SCOUT lazy hybrid MCP tool discovery ADOPT policy (K311) · StepGuard step-level guardrails ADOPT policy (K312) · StarHarness stratified harness evolution ADOPT protocol, MIT REFERENCE clone (K313) · Recuris EM–WM memory coupling ADOPT pattern, Apache-2.0 REFERENCE clone (K314).
## Body
1. **K310 Beyond the Mandate** (2608.23858): valid mandate signatures ≠ user intent when pre-auth context is manipulated — A2A messages and MCP tool results that shape a transaction stay unsigned. 48 threats / 8 High (AIVSS) across five deployment architectures; MAESTRO model; v0.2 HNP flows widen surface. Policy: bind pre-commit context (pairs K302 revalidate-at-commit). Dual-ID vs CCC K285 Mandato — do not merge. **No PoC/exploit text anywhere**; no testbed/scanner clone. `policy_wired`. Cybersec steal brief written.
2. **K311 SCOUT** (2608.23992): enterprise MCP proxy with 200+ servers / 2k tools saturated 140.2k tokens before turn one; two meta-tools (`tool_search` hybrid BM25+dense RRF top-k → `execute_tool`) cut it to 1.3k (99%), zero client changes across six clients incl. Claude Code/Cursor. Prompt caching does not fix context saturation. Steal: never load the full tool catalog at turn 0 (pairs K164 lazy stacks, BPS set-not-top-k). No public repo → `policy_wired`. Atto steal brief written.
3. **K312 StepGuard** (2608.24777): step-level guard checks candidate tool actions **before execution** + post-trajectory audit; StepGen builds context-matched safe/unsafe pairs diverging only at the risky step; Balance-GRPO tunes the safety–utility dial explicitly (ASR −77.3%, utility −2.8 pts on AgentDojo/AgentDyn). Gate the step, not only the final answer (pairs K239 preview). `zheng977/StepGuard` license:null → NO-GO clone; trainer `wont_wire`, policy `policy_wired`.
4. **K313 StarHarness** (2608.24804): evolves harnesses (prompts/tools/skills/MCP/subagents/loop) with weights frozen; pool stratified by baseline failure mode; proposer-visible search ≠ proposer-hidden selection ≠ held-out eval; 4–12 accepted changes → +20–35 pp across ITBench/EnterpriseOps-Gym/AutomationBench; transfers GPT↔Qwen without re-evolution. MIT clone `.local/adopts/StarHarness` (24MB <500MB); trainer `wont_wire`; do not auto-evolve `.cursor/skills` (pairs K281/K292/K298/K307).
5. **K314 Recuris** (2608.24876): Working Memory tracks task state and picks skills from Experiential Memory; traces localize failures to a specific memory component; Meta-Agent applies validation-gated Skill Memory patches (+17.8 pp τ²-Bench GPT-5.6 Sol; +32.2 pp longest horizon; six failure modes −20–86%). Apache-2.0 clone `.local/adopts/Recuris` (4MB); runtime `wont_wire`; HITL before any skill self-write (pairs K309/K237).
## Sources
arXiv 2608.23858 / 23992 / 24777 / 24804 / 24876 — all archived to `cemini-egress-fi:/opt/cemini-bulk/research/ccc/`
